{
  "name": "node-forward-proxy",
  "version": "1.0.0",
  "description": "Windows-compatible HTTP/HTTPS forward proxy with Redis Cluster cache",
  "main": "dist/proxy.js",
  "type": "module",
  "scripts": {
    "build": "tsc",
    "start": "node dist/proxy.js",
    "dev": "node --watch --experimental-strip-types proxy.ts"
  },
  "dependencies": {
    "ioredis": "^5.3.2"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "typescript": "^5.4.0"
  },
  "engines": {
    "node": ">=18"
  }
}
